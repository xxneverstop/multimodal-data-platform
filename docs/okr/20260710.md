# 2026-07-10 工作总结：MOTION_PHYSICS_METRICS_V2 Pipeline 实现与 motion 能力整合方案

## 完成事项

1. **更新了 CLAUDE.md 项目指引**，新增本地开发环境说明（远端数据+本地代码模式、"后端读 .env，Worker 读环境变量"的配置机制差异）、V2 Pipeline 验证方式、Worker 新增环境变量（`MMDP_SMPL_MODEL_DIR`、`MMDP_PHYSICS_DEVICE`），以及远端 MySQL 地址等关键信息

2. **为 motion_vis 和 MotionDB 构建了 codebase-memory 知识图谱索引**，motion_vis（675 节点/1664 边）、MotionDB（4992 节点/17542 边），为能力整合提供代码级引用和跨项目调用链追踪

3. **撰写了 motion_vis + MotionDB → MMDP 整合方案文档**（`docs/processing/motion-integration-plan.md`），系统分析了两个源项目的能力全景，梳理了已完成集成（BUILD_MOTION_VIEWER_DATA、MOTION_PHYSICS_METRICS V1、MotionViewer.vue）和待整合能力（按 P1-P3 优先级分类），明确了"主框架不动、功能插入现有架构槽位"的整合策略，并深入分析了 torch 依赖的影响范围——仅 Worker 端需要，算法从 motion_vis 搬运非研发

4. **实现了 MOTION_PHYSICS_METRICS_V2 Pipeline**（`mmdp-worker/pipelines/motion_physics_metrics_v2.py`，约 470 行），从 motion_vis 的 `phys_metrics.py`（743 行）移植了完整物理指标计算链路：`load_motion_data` → `BodyModel.forward()` → 12 指标输出（jitter、avg_penetrate_mm、avg_float_mm、avg_skate_mm、skate_ratio、joint_pop/arms/legs/wrists/ankles_pop_ratio、wrist_twist_ratio、phys_err_mm）。配套完成：
   - 拷贝 `body_model/` 包（`lbs.py` 170 行 + `body_model.py` 222 行 + `__init__.py`）至 `mmdp-worker/pipelines/` 和 `deploy/worker/src/pipelines/`，包导入链路验证通过
   - 后端 `AssetType.java` 新增 `PHYSICS_REPORT_V2` 枚举值，前端 `asset.ts` 补全 5 个缺失的 motion 相关类型
   - 更新 Dockerfile（torch CPU 版安装 + SMPL 模型 COPY）和 `requirements.txt`
   - Worker `config.py` 新增 `.env` 自动加载机制（`os.path.exists` + `os.environ.setdefault`），本地开发无需每次手动 `set` 环境变量，服务器部署时 Docker Compose 注入的值优先保留
   - 创建 `mmdp-worker/.env.example` 模板文件

5. **撰写了 V2 Pipeline 验证与部署指南**（`docs/processing/motion-v2-verify-deploy.md`），区分了算法级测试（纯 Python，不依赖后端/DB/OSS，直接调用 `_compute_metrics_v2()`）和 Worker 级集成测试（需要远端 MySQL + OSS + 后端在线，走完整轮询-领取-执行-上报链路），覆盖了服务器 Docker Compose 部署全流程（SMPL 模型准备、镜像构建、数据库注册、环境变量配置）及 5 个常见故障排查场景（torch 未安装、模型路径错误、OOM、指标异常等）

6. **为后端处理作业链路添加了关键日志**，在 `ProcessingJobServiceImpl` 的 6 个关键方法中添加了 12 条日志（`[处理作业]`/`[Worker]` 前缀），覆盖三种作业创建路径（MOCK / MANUAL / PYTHON_WORKER）、Worker 领取、成功上报、失败上报全链路。排查过程中定位了"BUILD_PLAYBACK 直接成功"根因（走了手动登记路径导致状态立即 SUCCESS）和"MOTION_PHYSICS_METRICS_V2 Unknown pipeline"根因（Worker 进程未重启导致内存 PIPELINES dict 未刷新）

7. **完成 V2 Pipeline 端到端验证**，在本地环境（pose_env + 远端 MySQL + 远端 OSS）产出首个 `PHYSICS_REPORT_V2` JSON 报告，12 个指标全部有数值。诊断了 `avg_penetrate_mm=2326mm` 异常为 NPZ 数据未做根节点归零导致（坐标系偏移），非算法问题，确认 Pipeline 逻辑正确

## 下步计划

1. 在 V2 Pipeline 的 `_load_motion_data()` 中添加 trans 首帧归零预处理（`trans[:, 1] -= trans[:1, 1]`），解决坐标系偏移导致的穿透异常
2. 推进 P1 优先级整合项：动作标注系统（新表 `motion_annotation` + CRUD API + MotionViewer 标注面板）
3. 服务器 Docker 环境部署验证（需先准备 SMPL-H 模型文件放入 `deploy/worker/smpl_models/`）
4. 优化 MotionViewer 前端：增加物理指标报告展示面板、文件排序功能

---

# 2026-07-10（下午/晚间）：动作标注系统实现与 MotionViewer UX 重构

## O（object，待办版本）

1. 完成动作标注系统后端搭建（数据表 + API）
2. 完成 MotionViewer 页面布局重构（对齐 PlaybackView）
3. 完成双层时间轴组件（标注轨道 + 进度条 + 悬停信息）
4. 完成标注数据模型扩展（MotionDB 缺陷字段 + 帧段支持）
5. 完成标注创建/编辑 Modal 与侧边栏 UX 优化
6. 完成 Session 详情页标注进度集成

## 完成事项

1. **搭建了动作标注系统后端完整链路**：新建 `motion_annotation` 表（与 `data_asset` 一对一）、`AnnotationStatus` 枚举、`MotionAnnotation` Entity/Mapper/Service/Controller（5 个 API 端点：获取标注、通过 fileId 查找、保存/更新、Session 进度、Task 进度），含乐观锁版本冲突检测和 JSON 字段序列化。新增 `annotation` 模块 package，遵循项目现有的 Controller→Service→Mapper 分层架构

2. **重构了 MotionViewer 页面布局**（~500 行重写）：从独立全屏 3D 查看器改为 PlaybackView 风格布局（`100vh` + system-ui 字体），左侧 240px 侧边栏使用 4 个 tab（动作列表/设置/标注/信息），底部集成双层时间轴组件，删除了旧的右侧 360px 抽屉式 AnnotationPanel.vue，将标注功能全部迁移到侧边栏标注 tab 中

3. **开发了 MotionTimeline 双层时间轴组件**（280 行）：上层标注轨道层（render 帧段标注为彩色条块，支持拖拽选区创建标注段）；下层进度条层（视觉进度条 + 透明 range input 叠加 + 悬停竖线 + frame tooltip，显示帧号/时间/该帧标注问题摘要）；底部控制栏统一了播放/跳帧/循环操作

4. **扩展了标注数据模型**：后端 `FrameIssueItem` 新增 `endFrame`（支持帧段标注，null 表示单帧）和 `defectType`（MotionDB 缺陷类型枚举）；`motion_annotation` 表新增 `motiondb_defects`（8 个 MotionDB 风格缺陷字段：testSet/flatScene/jointJump/jointDeformity/sliding/floatPenetrate/displacementMissing/temporalConsistency）和 `text_descriptions`（多条文本描述）；生成独立 SQL 迁移文件供手动执行

5. **开发了 AnnotationModal 居中标注编辑弹窗**：支持 create（新增）和 edit（编辑）两种模式，表单包含标注范围（起始帧/结束帧 + 单帧标注勾选）、8 个缺陷类型 chips 单选、严重程度 radio（低/中/高）、描述 textarea。创建入口 3 个：时间轴拖拽选区 → modal、快捷键 N（标记当前帧）→ modal、快捷键 M（标记入/出点）→ modal、侧边栏"+新增"按钮 → modal

6. **重设计了侧边栏标注 Tab UX**：将帧级标注列表提升为主体区域（左侧颜色条标识 severity + 帧范围 + 类型标签 + ✎编辑/×删除按钮），文件级属性（MotionDB 缺陷评估 + 动作标签 + 文本描述）折叠到"动作属性"可展开区域，整体信息层次清晰

7. **集成了 Session 详情页标注进度展示**：侧边栏新增"动作标注进度"卡片（进度条 + 评级分布 `A:5 B:12`），AnnotationTaskView 标注列表页替换 mock 数据为真实 Session 标注进度查询

## 下步计划

1. 执行数据库迁移 SQL（`migration_add_motion_annotation.sql` + `migration_add_motiondb_defects.sql`）至远端 MySQL
2. 启动完整链路端到端验证：3D 查看 → 标注创建 → 保存 → 刷新持久化 → 版本冲突 → Session 进度聚合
3. 优化 MotionViewer 标注卡片展示：增加"标注轨道层与帧级列表双向高亮联动"
4. 后续增加标注段边缘拖拽调整起始帧功能（类似剪辑软件）
