> 

# 2026-07-03 工作记录

O（object，待办版本，对应后面完成事项）：

1. 完成 Python Worker 容器化部署方案设计与全部物料落地
2. 排查并修复构建链路中的阻塞问题，打通 deploy 全流程


完成事项：

1. 完成了 v0.4.0 部署方案（`deploy/docker-compose.yml` 新增 `mmdp-worker` 服务、`deploy/worker/Dockerfile` 含国内镜像源、`.env.example` 补全 Worker 变量、`copy-deploy.bat` 新增步骤 6 打包 Worker 源码），并输出完整部署文档 `docs/mmdp-deploy-v0.4.0.md`（12 章，含架构图、运维专题、v0.3.0 升级指南）

2. 修复了打包链路中 2 个阻塞问题——后端测试类 `ProcessingJobServiceImplTest` 构造函数参数不匹配（补齐 6 个新依赖注入的 mock）和前端 12 个 TypeScript 类型错误（`AssetType` 缺 3 个枚举值、`AssetListItem` 缺 `sourceKey`、三.js 类型兼容、PlaybackView 模板 `window` 访问和空值处理等），`copy-deploy.bat` 现已可完整执行

3. 排查了 admin 登录失败问题，确认数据库中 BCrypt 哈希与密码不匹配为直接原因；发现系统缺少默认管理员初始化机制（鸡生蛋问题），创建了 `DataInitializer` 启动自动检查并创建默认账号；排查了服务器部署中 Docker Hub 超时问题，将 Worker Dockerfile 切为阿里云 + 清华国内源

4. 输出了 3 篇配套文档——`mmdp-cicd-learner-guide.md`（从学习者视角讲 CI/CD，GitLab CI + ACR + Docker Compose 方案）、`env-management-guide.md`（三层分离原则：敏感/非敏感、本地/部署、dev/prod，含 CI/CD 场景下的 GitLab Variables 管理）、`mmdp-cicd-plan.md`（含 K8s 升级路径与成本估算）


下步计划（v0.4.0 部署）：

1. 轮换已通过 Git 历史泄露的 OSS AccessKey，清理 `mmdp-worker/config.py` 硬编码默认值，将服务器 `.env` 补全 Worker 变量后完成 v0.4.0 首次全量部署验证

---

## motion_vis 3D 动作可视化功能接入

O（object）：

1. 完成 motion_vis 功能接入方案调研与技术路线设计
2. 推进 ACCAD SMPL 数据全链路接入（Profile → Session 导入 → Pipeline 处理）
3. 实现动作物理指标计算 Pipeline（MOTION_PHYSICS_METRICS）
4. 实现 3D 查看数据生成 Pipeline（BUILD_MOTION_VIEWER_DATA）
5. 搭建前端 3D 动作查看器（Three.js + Vue3），经多轮迭代定版
6. 扩展 Session 详情页 DataFile 展开列表功能
7. 修复接入过程中的数据格式、加载卡死、方向错误等关键问题

完成事项：

1. 完成了 motion_vis 功能接入方案调研与技术路线设计——深入分析了 motion_vis（Flask + Three.js SMPL 查看器）和 MotionDB（标注系统）的功能架构与数据契约；确定核心决策：SMPL NPZ 原生数据直接用于 3D 查看（frontend_mesh 模式），FBX 仅作为导出格式；设计了 Python Worker Pipeline 做数据预处理、Java 后端只做路由、前端 Three.js 做渲染的分层架构

2. 推进了 ACCAD SMPL 数据全链路接入——新增 4 个 AssetType（SMPL_NPZ、PHYSICS_REPORT、MOTION_VIEWER_JSON、MOTION_FBX）；编写数据准备脚本将 5 个 NPZ 文件组织为标准 Session 目录结构；编写完整数据库迁移 SQL（Profile + Source + 3 个 Pipeline + 关联关系）；解决了一系列上传流程问题（manifest 字段位置不匹配、Profile rule_code 无效、逻辑删除+唯一索引冲突、目录 source 单 DataAsset 限制）

3. 实现了动作物理指标计算 Pipeline——`motion_physics_metrics.py`，纯 numpy 计算抖动、地面穿透、浮空比、位移范围等指标，产出 PHYSICS_REPORT JSON

4. 实现了 3D 查看数据生成 Pipeline——`build_motion_viewer_data.py`，将 SMPL NPZ 转换为与 motion_vis 完全兼容的帧数据 JSON，自动检测 SMPL/SMPLH/SMPLX 类型

5. 搭建了前端 3D 动作查看器，经三轮迭代定版——安装 three.js，复制 SMPL 模型静态资源（14MB，7 个模型目录）；第一版单文件查看器，移植 motion_vis 的 SkinnedMesh + axis-angle→quaternion 骨骼驱动逻辑；第二版多模型并排对比（单场景 X 轴偏移），因 UX 不佳废弃；第三版（定版）单视图 + 侧边栏文件切换器 + 模型池预加载，切换文件瞬间完成；实现增强骨骼可视化、8 色调色板、播放控制、OrbitControls

6. 扩展了 Session 详情页 DataFile 展开列表——后端新增 `GET /api/sessions/{sessionId}/files` 端点，前端资产行增加展开箭头显示底层文件清单

7. 修复了接入过程中的关键问题——加载卡死（canvasWrap DOM 未挂载）、首模型不显示（selectedId 初始值冲突）、相机方向错误、页面刷新重下载（Cache API 缓存）、并行下载+进度条修正

涉及文件：
- 新建：`motion_physics_metrics.py`、`build_motion_viewer_data.py`、`prepare_session_dir.py`、`migration_add_motion_support.sql`、`MotionViewer.vue`
- 修改：`AssetType.java`（+4）、`DataFileService.java`（+1 方法）、`DataFileServiceImpl.java`（+实现）、`DataFileController.java`（+端点）、`sessions.ts`（+2 API）、`SessionDetailView.vue`（+DataFile 展开+入口按钮）、`router/index.ts`（+路由）、`main.css`（+样式）、`package.json`（+three.js）
- 复制：SMPL 模型 .bin 文件到 `backend/static/assets/` 和 `frontend/public/assets/`

下步计划（motion_vis 接入）：

1. 接入 MotionDB 标注能力到平台 annotation 模块
2. 实现 FBX/BVH/SOMA 格式的 3D 查看支持
3. 创建 Motion3DPlaybackRuleResolver，Session 详情页直接显示入口
4. 实现 NPZ→FBX 导出 Pipeline（纯 Python ASCII FBX）
